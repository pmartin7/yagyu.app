# Vercel NFT + includeFiles cannot reliably preserve pnpm's isolated nested
# symlinks (analyze fails on the next missing transitive: zod/v4,
# eventsource-parser, …). Hoist so AI SDK packages resolve as real
# node_modules entries in /var/task.
node-linker=hoisted
